# CLAUDE.md — הנחיות עבודה עבור Claude Code בפרויקט זה

מסמך זה מנחה כל מופע של Claude Code שעובד על המאגר הזה, כולל את שני השותפים שעובדים
כל אחד עם חשבון ה-Claude Code שלו.

## סיכום ארכיטקטורה
טרם נקבעה. הארכיטקטורה תיקבע ותתועד כאן **לפני** תחילת כתיבת קוד עסקי, כחלק מתכנית
הפיתוח שתאושר על ידי בעל/י המוצר. הסעיף הזה יעודכן עם: שכבת ה-Frontend, שכבת ה-Backend/API,
מסד הנתונים, שכבת ההרשאות/האימות, ואופן הפרסום.

**כלל מחייב:** לפני כל שינוי מהותי (פיצ'ר חדש, שינוי סכמה, שינוי הרשאות) — יש לקרוא את
מסמך האפיון המלא של המוצר ואת הארכיטקטורה המתועדת כאן.

## כללי פיתוח
- לפני התחלת עבודה: למשוך (`pull`) את הגרסה העדכנית מהענף הרלוונטי.
- כל עבודה מתבצעת בענף נפרד שנוצר מ-`main` (ראו `CONTRIBUTING.md` למוסכמות שמות).
- לעולם לא לעבוד ישירות על `main`.
- לבצע Commit לאחר כל יחידת עבודה הגיונית, עם הודעה ברורה שמסבירה **למה** בוצע השינוי.
- כל שינוי עולה כ-Pull Request, גם אם בוצע על ידי Claude Code.
- **אין לבצע שינוי הרסני** (מחיקת ענפים, איפוס היסטוריה, מחיקת קבצים משמעותיים, מחיקת
  נתונים) **בלי אישור מפורש** של בעל המשימה.
- אחרי כל שינוי קוד: להריץ את הבדיקות הקיימות (Lint / Type-check / Unit / Integration,
  בהתאם למה שמוגדר באותו שלב) ולוודא שהן עוברות לפני שמדווחים שהמשימה הושלמה.
- בסיום כל משימה: להסביר בשפה פשוטה וברורה (לא מפתחים מקצועיים) **מה השתנה, למה, ואיך
  אפשר לבדוק את זה**.
- שני שותפים עובדים על אותו מאגר במקביל — לפני תחילת עבודה חדשה תמיד למשוך שינויים
  עדכניים, ולעולם לא לדרוס עבודה של השותף (ראו `CONTRIBUTING.md` לטיפול בהתנגשויות).

## כללי אבטחה
- אסור בהחלט לשמור סיסמאות, מפתחות API, Tokens או פרטי חיבור למסד נתונים בקוד או ב-Git.
  שימוש אך ורק במשתני סביבה ובמנגנון ה-Secrets של סביבת הפרסום.
- אסור להדפיס סודות ללוגים, לפלט קונסולה או להודעות בשיחה.
- כל בדיקת הרשאה (מי מותר לו לראות/לערוך מה) חייבת להתבצע **בצד השרת**. הסתרת כפתור
  בממשק אינה הגנה.
- עיקרון הרשאה מינימלית: כל תפקיד מקבל רק את מה שהוא באמת צריך.
- פעולות רגישות (מחיקה, שינוי הרשאות, גישה לנתוני לקוח, תשלומים) נרשמות ל-Audit Log.
- העלאת קבצים: לבדוק סוג קובץ, גודל מקסימלי, ולוודא הרשאת המשתמש להעלות/לצפות בקובץ.
- אסור להשתמש בנתוני לקוחות אמיתיים בפיתוח ובבדיקות — רק נתוני דמו פיקטיביים לחלוטין.
- כל שינוי בסכמת מסד הנתונים מתבצע דרך Migration מתועד, וככל האפשר הפיך (עם סקריפט
  Rollback).
- יש לבדוק תלויות (בהתאם למחסנית שתיבחר) מול חולשות אבטחה ידועות באופן שוטף.
- לפני כל עלייה לסביבת ייצור — יש להריץ סקירת/בדיקת אבטחה.
- להכין ולתחזק מנגנון גיבוי ושחזור למסד הנתונים.

## כללי RTL ועברית
- ממשק המשתמש כולו בעברית, RTL (מימין לשמאל), פשוט ונגיש — קהל היעד הם עובדי מגרש
  רכבים שמורגלים בעיקר לעבודה עם Excel, לא אנשי מחשבים.
- להעדיף שפה פשוטה וברורה על פני מונחים טכניים, גם בהודעות שגיאה ובטקסטים בממשק.
- לוודא תמיכה נכונה ב-RTL בכל רכיב חדש: יישור, כיווניות אייקונים, טפסים, טבלאות,
  תאריכים ומספרים.
- קוד המקור (משתנים, פונקציות, הערות) — באנגלית, כמקובל. רק מה שהמשתמש רואה בממשק —
  בעברית.

## הוראות עבודה כלליות
- לקרוא את מסמך האפיון המלא לפני כל שינוי מהותי בתכנון או בארכיטקטורה.
- לא לבצע שום שינוי הרסני (מחיקה, Force Push ל-`main`, שינוי סכמה בלתי הפיך) בלי אישור
  מפורש.
- להריץ בדיקות לאחר כל שינוי ולפני כל בקשת מיזוג.
- להסביר כל שינוי בשפה פשוטה, כולל: מה נבנה, איך בודקים אותו, ומה השלב הבא.
