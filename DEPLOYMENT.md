# פרסום המערכת לאינטרנט (Railway)

מדריך זה מיועד לבעל המוצר (לא איש מחשבים) — הוראות מדויקות, שלב-אחר-שלב, להעלאת
המערכת ל-[Railway](https://railway.app) כדי שגם השותף שלך יוכל לראות ולהשתמש בה
דרך קישור באינטרנט, בלי להתקין שום דבר במחשב.

**חשוב לדעת מראש:**
- אלה **נתוני דמו בלבד** (לקוחות ורכבים פיקטיביים) — אסור להזין נתוני לקוחות אמיתיים
  לפני שעוברים לשלב הבא (סליקה אמיתית, גיבויים וכו', ראו `docs/architecture.md`).
- אני (Claude) לא יכול ליצור חשבון או להזין פרטי תשלום בשבילך בשירות חיצוני — כל שלב
  שדורש כניסה לאתר Railway עצמו הוא שלב שאתה מבצע. אני כבר הכנתי את כל הקבצים וההגדרות
  בקוד כדי שהתהליך יהיה פשוט ככל האפשר.
- שום סיסמה או מפתח לא אמור להיכתב בקוד או ב-Git — בכל מקום שמופיע "משתנה סביבה"
  (Environment Variable) למטה, מזינים אותו ישירות באתר Railway, לא בקובץ בפרויקט.

## שלב 0 — הרשמה ל-Railway
1. כניסה ל-[railway.app](https://railway.app) ולחיצה על "Login" / "Start a New Project".
2. הכי פשוט: להתחבר עם חשבון GitHub (אותו חשבון שבו נמצא המאגר `Car-Lot-Management`).
3. Railway עשוי לבקש לאשר גישה למאגרים שלך ב-GitHub — יש לאשר גישה למאגר הזה בלבד
   (או לכל המאגרים, לפי מה שהאתר מציע).

## שלב 1 — יצירת פרויקט ומסד נתונים
1. לוחצים על **New Project**.
2. בוחרים **Provision PostgreSQL** (מוסיף מסד נתונים מנוהל — Railway יוצר עבורו
   אוטומטית משתנה בשם `DATABASE_URL`, לא צריך להזין שם/סיסמה בעצמכם).

## שלב 2 — הוספת שירות ל-Backend
1. באותו פרויקט: **New** → **GitHub Repo** → בוחרים ב-`Car-Lot-Management`.
2. Railway ייצור שירות חדש. נכנסים ל-**Settings** של השירות הזה:
   - **Root Directory**: `backend`
   - **Build Command**: `npm run build` (אם יש שדה נפרד; אחרת Railway מזהה אוטומטית)
   - **Start Command**: `npm run start:prod` — **חשוב לוודא שזה מוגדר בדיוק כך**, כי
     הפקודה הזו גם מריצה את ה-Migrations למסד הנתונים וגם מפעילה את השרת.
3. בלשונית **Variables** של השירות מוסיפים:
   | שם המשתנה | ערך |
   |---|---|
   | `DATABASE_URL` | להפנות למשתנה של שירות ה-Postgres (Railway בד"כ מציע את זה אוטומטית כ-Reference; אם לא — מעתיקים אותו מלשונית ה-Variables של שירות ה-Postgres) |
   | `JWT_ACCESS_SECRET` | ערך אקראי וסודי, **לא** ערך שאני נותן לך בשיחה. הכי בטוח: לפתוח טרמינל במחשב שלך ולהריץ `openssl rand -base64 32` (ב-Mac/Linux) או להשתמש במחולל סיסמאות אקראי, ולהדביק את התוצאה כאן ישירות. |
   | `JWT_ACCESS_EXPIRES_IN` | `15m` |
   | `SEED_OWNER_PASSWORD` | סיסמה חזקה לבחירתך למשתמש הראשי (owner@demo.local). שמרו אותה במקום בטוח — **אין כרגע מסך בממשק לשינוי סיסמה**, אז זו הסיסמה שתשתמשו בה. |
   | `PORT` | אין צורך להגדיר — Railway מגדיר אוטומטית, והשרת כבר קורא אותו. |
4. שומרים — Railway יתחיל Deploy אוטומטית. אפשר לעקוב בלשונית **Deployments**.
5. בלשונית **Settings → Networking** לוחצים **Generate Domain** — מקבלים כתובת ציבורית
   כמו `car-lot-backend-production.up.railway.app`. **שומרים את הכתובת הזו**, נצטרך אותה
   בשלב הבא.
6. בדיקה: פותחים בדפדפן `https://<הכתובת-שקיבלתם>/health` — אמור להופיע `{"status":"ok"}`.

## שלב 3 — הוספת שירות ל-Frontend
1. באותו פרויקט: **New** → **GitHub Repo** → שוב `Car-Lot-Management` (שירות נוסף,
   נפרד מה-Backend).
2. ב-**Settings** של השירות הזה:
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Start Command**: `npm start`
3. בלשונית **Variables** מוסיפים:
   | שם המשתנה | ערך |
   |---|---|
   | `VITE_API_URL` | הכתובת של ה-Backend מסעיף 2.5, עם `https://` בהתחלה — למשל `https://car-lot-backend-production.up.railway.app` |

   **חשוב:** משתנה זה "נאפה" לתוך הקבצים בזמן ה-Build, אז חייבים להגדיר אותו **לפני**
   שה-Deploy הראשון רץ. אם משנים אותו אחר כך — צריך Deploy מחדש (Redeploy) כדי שהשינוי ייכנס.
4. שומרים, נותנים ל-Deploy לרוץ, ואז ב-**Settings → Networking** לוחצים **Generate Domain**
   גם כאן — זו הכתובת שתשלחו לשותף שלכם.

## שלב 4 — סגירת המעגל (אבטחה)
חוזרים לשירות ה-**Backend**, ל-**Variables**, ומוסיפים:

| שם המשתנה | ערך |
|---|---|
| `FRONTEND_URL` | הכתובת הציבורית של ה-Frontend מסעיף 3.4 |

זה מגביל את השרת לקבל בקשות רק מהאתר שלכם (ולא מכל אתר באינטרנט). שומרים — ה-Backend
יעשה Deploy מחדש אוטומטית.

## שלב 5 — יצירת המשתמש הראשי (Seed)
המערכת עדיין ריקה — צריך ליצור פעם אחת את הארגון, הסניף והמשתמש הראשי. הדרך הפשוטה
ביותר היא דרך Railway CLI (תוכנה קטנה שמתקינים פעם אחת במחשב):

```bash
npm install -g @railway/cli     # התקנה חד-פעמית
railway login                   # ייפתח דפדפן לאישור
railway link                    # בוחרים את הפרויקט שיצרתם
railway run --service backend npm run prisma:seed
```

בסיום ההרצה יודפס `owner@demo.local` כמשתמש הבעלים — הסיסמה היא מה שהגדרתם
ב-`SEED_OWNER_PASSWORD` בשלב 2.

## שלב 6 — בדיקה
1. פותחים את כתובת ה-Frontend מסעיף 3.4.
2. מתחברים עם `owner@demo.local` והסיסמה שבחרתם.
3. עוברים על המסכים (רכבים, לקוחות וכו') — כל הנתונים שתראו הם נתוני דמו.

## תחזוקה שוטפת
- כל `git push` ל-`main` (למשל אחרי מיזוג Pull Request) גורם ל-Railway לבנות ולפרסם
  מחדש אוטומטית את שני השירותים — אין צורך בפעולה ידנית.
- שינוי סכמת מסד נתונים (Migration חדש) מתבצע אוטומטית בכל עליית Backend
  (`prisma migrate deploy` בתוך `npm run start:prod`), בהתאם לכלל ב-`CLAUDE.md`
  שדורש Migrations מתועדים.
- **פער ידוע:** אין כרגע מסך "שינוי סיסמה" בממשק. עד שייבנה, שינוי סיסמת משתמש קיים
  אפשרי רק ישירות במסד הנתונים (פעולה טכנית) — מומלץ לבקש זאת כמשימה נפרדת לפני שנותנים
  גישה לעובדים אמיתיים מעבר לכם ולשותף.
