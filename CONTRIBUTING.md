# CONTRIBUTING — תהליך עבודה בפרויקט

מסמך זה מגדיר איך שני השותפים (וכל מי שעובד מטעמם, כולל Claude Code) עובדים יחד על
המאגר בלי לדרוס אחד את השני.

## עקרון-על
- `main` = תמיד יציב וניתן לפרסום. **אין לעבוד ישירות על `main` ואין לבצע Push ישיר אליו.**
- כל שינוי, כולל שינוי קטן, מתבצע בענף נפרד ועולה כ-Pull Request.

## איך פותחים ענף חדש
1. למשוך עדכונים: `git checkout main && git pull origin main`
2. ליצור ענף חדש מ-`main`, בשם ברור לפי הדפוס:
   - `feature/<שם-הפיצ'ר>` — פיצ'ר חדש, למשל `feature/vehicle-inventory`
   - `fix/<שם-התקלה>` — תיקון באג, למשל `fix/login-permissions`
   - `chore/<תיאור>` — עבודת תשתית/תחזוקה שאינה פיצ'ר, למשל `chore/update-dependencies`
   - `docs/<תיאור>` — שינוי בתיעוד בלבד
3. `git checkout -b feature/vehicle-inventory`

## איך מבצעים Commit
- Commit אחד = יחידת עבודה הגיונית אחת שאפשר להסביר במשפט.
- הודעת ה-Commit מסבירה **למה** בוצע השינוי, לא רק מה השתנה.
- לדוגמה: `הוספת מסך חיפוש רכבים כדי לאפשר לעובדים למצוא רכב לפי מספר רישוי`
- אסור לכלול ב-Commit סיסמאות, מפתחות או נתוני לקוח אמיתיים.

## איך פותחים Pull Request
1. לדחוף את הענף: `git push -u origin <שם-הענף>`
2. לפתוח Pull Request כלפי `main` ב-GitHub — תבנית ה-PR נטענת אוטומטית.
3. למלא את כל השדות בתבנית: מה השתנה, למה, איך נבדק, השפעה על מסד הנתונים, השפעה על
   הרשאות/אבטחה, צילומי מסך (אם רלוונטי), הוראות Rollback.

## בדיקות חובה לפני מיזוג
- כל הבדיקות האוטומטיות (כשיוגדרו בפרויקט) חייבות לעבור בירוק.
- לוודא שאין קבצי סוד (`.env`, מפתחות, טוקנים) בשינוי.
- אם יש שינוי בסכמת מסד נתונים — לוודא שיש Migration מתועד וסקריפט Rollback.

## תהליך סקירה ומיזוג
- כל Pull Request עובר סקירה (Code Review) — עדיף על ידי השותף השני, במיוחד בשינוי משמעותי.
- יש לפתור (Resolve) את כל הערות הסקירה לפני מיזוג.
- מיזוג ל-`main` רק לאחר שהבדיקות עברו והסקירה אושרה.
- אין לבצע Force Push ל-`main`, ואין למחוק ענפים, היסטוריה, מסד נתונים או קבצים
  משמעותיים ללא אישור מפורש.

## התנגשויות (Conflicts)
אם יש התנגשות בין השינויים שלך לשינויים שכבר במאגר:
1. לעצור ולא למחוק אף גרסה.
2. למשוך את `main` העדכני לענף שלך: `git fetch origin main && git merge origin/main`
   (או `git rebase origin/main` אם כך מוסכם בצוות).
3. לפתור את ההתנגשות ידנית תוך שמירה על שני השינויים במידת האפשר, ולתאם עם השותף אם
   לא ברור מה נכון.
4. להריץ בדיקות שוב לפני שממשיכים.

## עבודה עם Claude Code על אותו מאגר
- לפני שמתחילים משימה חדשה: `git checkout main && git pull`, ואז ליצור ענף חדש למשימה.
- לתת ל-Claude Code הקשר על מה השותף השני עשה לאחרונה (למשל: "השותף עובד על ניהול
  לידים בענף feature/leads") כדי למנוע כפילות עבודה.
- כל אחד עובד על הענף/הפיצ'ר שלו ולא נוגע בענפים של השותף בלי תיאום.
- לפני מיזוג, לוודא שהענף מעודכן מול `main` כדי למנוע התנגשויות גדולות.
