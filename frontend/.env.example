# בייצור: הכתובת הציבורית של ה-Backend. חשוב — Vite "אופה" את הערך הזה
# לתוך קבצי ה-Build בזמן npm run build, אז צריך להגדיר אותו נכון *לפני*
# הבנייה (לא ניתן לשנות אחרי ה-Build בלי לבנות מחדש).
VITE_API_URL=http://localhost:3000
